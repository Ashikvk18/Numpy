import numpy as np

arr = np.array([1, 2, 3, 4, ])
print(arr.dtype) # dtype gives the data type of the array

arr2 = np.array([1, 2, 3, 4], dtype='i4')
print(arr2.dtype) # dtype gives the data type of the array